projection;
strict ( 2 );
use draft;

define behavior for ZC_TR_DEPARTMENT alias Dept
{
  use create;
  use update;

  use action deactivate;
  use action reactivate;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}