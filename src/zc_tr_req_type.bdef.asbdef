projection;
strict ( 2 );
use draft;

define behavior for ZC_TR_REQ_TYPE alias ReqType
{
  use create;
  use update;

  use action deactivateReqType;
  use action reactivateReqType;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}