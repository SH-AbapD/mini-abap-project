projection;
strict ( 2 );
use draft;

define behavior for ZC_TR_REQ_HDR alias Req
{
  use create;
  use update;

  use action approve;
  use action reject;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}